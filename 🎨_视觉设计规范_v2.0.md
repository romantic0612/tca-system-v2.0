# 🎨 TCA-System — 视觉设计规范 v2.0

> **来源**：基于 `TCA-System_技术规格说明书.md`（V2.0）重构，对齐 Vue3 + FastAPI + MySQL 8 新技术栈。
> **目标**：统一学生端 SPA、教师端 SPA、管理后台、登录页的视觉语言，覆盖四组实验条件（SA/EXP/AI-AUTO/TCA）的差异化 UI 表现。
> **设计依据**：[TCA-System 技术规格说明书 V2.0](TCA-System_技术开发规格说明书.md)

---

## 文档信息

| 属性 | 内容 |
|:---|:---|
| **文档编号** | TCA-DS-2026-001 |
| **版本号** | V2.0 |
| **发布日期** | 2026-05-15 |
| **文档状态** | 正式发布 |
| **技术栈** | Vue3 + Vite + Pinia + TypeScript / Element Plus / Socket.IO Client |
| **历史版本** | [v1.0](🎨_视觉设计规范_v1.0.md) — 基于 Streamlit 登录页提取 |

---

## 修订记录

|    版本    |       日期       | 修订内容                                                                                                  |
| :------: | :------------: | :---------------------------------------------------------------------------------------------------- |
|   V1.0   |       历史       | 基于旧版 Streamlit 登录页面 CSS 提取                                                                            |
| **V2.0** | **2026-05-15** | **全面重构：新增四组实验条件差异化UI设计、Guide/Tutor双Agent视觉系统、WebSocket实时通知系统、教师Override面板、Element Plus主题集成、前端组件结构对齐** |

---

## 目录

- [一、设计语言总览](#一设计语言总览)
- [二、色彩系统](#二色彩系统)
- [三、排版系统](#三排版系统)
- [四、组件规范](#四组件规范)
- [五、四组实验条件 UI 差异化设计](#五四组实验条件-ui-差异化设计)
- [六、登录页面布局](#六登录页面布局)
- [七、学生端页面](#七学生端页面)
- [八、教师端页面](#八教师端页面)
- [九、Agent 视觉系统](#九agent-视觉系统)
- [十、WebSocket 实时通知系统](#十websocket-实时通知系统)
- [十一、动画与过渡](#十一动画与过渡)
- [十二、Element Plus 主题集成](#十二element-plus-主题集成)
- [十三、全局样式与设计 Token](#十三全局样式与设计-token)
- [十四、组件实现清单](#十四组件实现清单)
- [十五、设计规范一致性检查](#十五设计规范一致性检查清单)
- [附录A：与旧版对照](#附录a与旧版-streamlit-到-vue3-对照)

---

## 一、设计语言总览

### 1.1 设计理念

- **教育感**：温暖、友好、值得信赖
- **科技感**：现代渐变、毛玻璃、平滑过渡
- **专注感**：减少视觉噪声，内容优先
- **适应性**：四组实验条件共享同一核心 UI，通过条件性渲染实现差异化

### 1.2 品牌信息

| 项目 | 值 |
|:---|:---|
| **系统名称** | **TCA-System**（基于人机协同的初中数学智能学习系统） |
| **英文名** | TCA-System (Teacher-Controllable AI) |
| **品牌标签** | 🎓 TCA-System · 华东师范大学 |
| **技术栈** | Vue3 + FastAPI + MySQL 8 |

### 1.3 核心角色与视觉关联

| 角色 | 视觉定位 | 品牌色关联 | 设计要点 |
|:---|:---|:---|:---|
| **学生** | 主要用户，对话交互 | 主色渐变背景 | 沉浸式对话体验 |
| **教师（TCA组）** | 监控者，Agent切换决策者 | Accent-Blue/Green | 实时数据面板，清晰状态展示 |
| **管理员** | 系统配置，数据导出 | 中性色 | 功能优先，简洁实用 |

---

## 二、色彩系统

### 2.1 主色板（保留 v1.0 品牌色，扩展）

| 色名 | 色值 | 用途 | 来源 |
|:---|:---|:---|:---|
| **Primary-400** | `#667eea` | 渐变起点、页面主背景起点、学生端对话面板背景 | 品牌色 |
| **Primary-500** | `#764ba2` | 渐变终点、品牌主色、AgentBadge-Guide | 品牌色 |
| **Primary-600** | `#5a3d8a` | 深色变体、激活态、导航栏激活态 | 衍色 |
| **Primary-100** | `#e8e0f4` | 浅色背景、hover、Guide Agent 消息高亮 | 衍色 |

### 2.2 Agent 专用色（v2.0 新增）

| 色名 | 色值 | 用途 |
|:---|:---|:---|
| **Agent-Guide** | `#667eea` → `#764ba2`（紫色渐变） | Guide（低支架/苏格拉底式提问）标签 |
| **Agent-Tutor** | `#2E86AB` → `#4CAF50`（青绿渐变） | Tutor（高支架/分步讲解）标签 |
| **Agent-Evaluator** | `#FF9800` → `#F44336`（橙红渐变） | Evaluator（评估）标签，仅日志记录 |

### 2.3 辅助色

| 色名 | 色值 | 用途 |
|:---|:---|:---|
| **Accent-Blue** | `#2E86AB` | 表单标题、按钮渐变起点、输入框聚焦边框、Tutor 关联色 |
| **Accent-Green** | `#4CAF50` | 按钮渐变终点、成功状态、Tutor 关联色 |
| **Accent-Red** | `#ff4d4f` | 错误提示、危险操作、TCA 待处理徽章 |
| **Accent-Orange** | `#FF9800` | 警告状态、等待中、触发检测通知 |
| **White** | `#FFFFFF` | 卡片背景、文字（暗底上） |

### 2.4 四组实验条件色（v2.0 新增）

用于教师端监控面板区分不同组别的学生状态标识：

| 组别 | 标识色 | 用途 |
|:---|:---|:---|
| **SA** | 灰色 `#9E9E9E` | 标识固定 Guide 组 |
| **EXP** | 蓝色 `#667eea` | 标识可解释组 |
| **AI-AUTO** | 青色 `#00BCD4` | 标识自动切换组 |
| **TCA** | 紫色 `#764ba2` | 标识教师可控组 |

### 2.5 中性色

| 色名 | 色值 | 用途 |
|:---|:---|:---|
| **Text-Primary** | `#333333` | 主要文字（亮底上） |
| **Text-Secondary** | `#666666` | 次要文字 |
| **Text-Light** | `rgba(255,255,255,0.9)` | 浅底文字 |
| **Text-Dim** | `rgba(255,255,255,0.85)` | 提示文字 |
| **Text-Muted** | `rgba(255,255,255,0.6)` | 极淡文字（时间戳、次要信息） |
| **Border** | `#E8E8E8` | 输入框边框、气泡边框 |
| **Border-Focus** | `#2E86AB` | 输入框聚焦边框 |
| **Chat-Bg** | `#f5f7fa` | 学生端对话面板背景（浅色） |

### 2.6 渐变定义

```scss
// 1. 页面背景渐变（主）— 学生端/登录页
$gradient-brand: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

// 2. 按钮渐变
$gradient-btn: linear-gradient(135deg, #2E86AB 0%, #4CAF50 100%);

// 3. Guide Agent 标签渐变
$gradient-guide: linear-gradient(135deg, #667eea, #764ba2);

// 4. Tutor Agent 标签渐变
$gradient-tutor: linear-gradient(135deg, #2E86AB, #4CAF50);

// 5. Evaluator (日志用)
$gradient-evaluator: linear-gradient(135deg, #FF9800, #F44336);

// 6. 毛玻璃背景（通用）
$glass-bg: rgba(255, 255, 255, 0.15);
$glass-bg-dark: rgba(255, 255, 255, 0.2);

// 7. 对话面板浅色渐变
$gradient-chat-bg: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);

// 8. 教师端面板（浅灰背景）
$gradient-teacher-bg: linear-gradient(135deg, #f0f2f5 0%, #e8ecf1 100%);

// 9. 消息气泡 — 用户
$gradient-user-bubble: linear-gradient(135deg, #2E86AB 0%, #4CAF50 100%);

// 10. 消息气泡 — AI (白色背景)
$bubble-ai-bg: white;
$bubble-ai-border: 2px solid #E8E8E8;
```

---

## 三、排版系统

### 3.1 字号层级

| 层级 | 大小 | 字重 | 用途 |
|:---|:---:|:---:|:---|
| Hero | `42px` | **Bold** | 品牌标题 H1（登录页） |
| H1 | `32px` | **Bold** | 页面主标题 |
| H2 | `24px` | **Bold** | 表单标题、区块标题、教师端段标题 |
| H3 | `20px` | **Bold** | 卡片标题、学生信息标题 |
| Body-Large | `18px` | Normal | 副标题、提示文字、按钮文字 |
| Body | `16px` | Normal | 正文、输入文字、对话内容 |
| Body-Small | `15px` | **600** | 输入框标签、Agent 名称 |
| Caption | `12px` | Normal | 品牌标签、版权信息、时间戳 |

### 3.2 文字阴影

```scss
// 主标题阴影（白色文字在渐变背景上）
$shadow-text-hero: 2px 2px 4px rgba(0, 0, 0, 0.2);

// 按钮阴影
$shadow-btn: 0 4px 15px rgba(46, 134, 171, 0.3);

// 按钮 hover 阴影
$shadow-btn-hover: 0 6px 20px rgba(46, 134, 171, 0.4);

// 卡片阴影
$shadow-card: 0 15px 50px rgba(0, 0, 0, 0.25);

// 教师端卡片阴影（轻量）
$shadow-teacher-card: 0 4px 15px rgba(0, 0, 0, 0.1);
```

### 3.3 内间距

| 场景 | 值 | CSS |
|:---|:---:|:---|
| 卡片内边距 | `40px` | `padding: 40px` |
| 毛玻璃容器 | `25px` | `padding: 25px` |
| 按钮内边距 | `14px 40px` | `padding: 14px 40px` |
| 输入框内边距 | `14px 16px` | `padding: 14px 16px` |
| 对话气泡内边距 | `12px 18px` | `padding: 12px 18px` |
| 表格单元格 | `8px 12px` | `padding: 8px 12px` |
| 品牌标签 | `8px 15px` | `padding: 8px 15px` |
| 学生卡片 | `15px` | `padding: 15px` |
| 导航栏 | `12px 24px` | `padding: 12px 24px` |

---

## 四、组件规范

### 4.1 卡片（Card）

| 属性 | 值 |
|:---|:---|
| 背景 | `white` |
| 圆角 | `border-radius: 20px` |
| 阴影 | `box-shadow: 0 15px 50px rgba(0,0,0,0.25)` |
| 最大宽度 | `max-width: 420px`（登录页）/ `100%`（内部页面） |
| 居中 | `margin: 0 auto` |

### 4.2 输入框（Input）

| 属性 | 值 |
|:---|:---|
| 圆角 | `border-radius: 12px` |
| 边框 | `2px solid #E8E8E8` |
| 内边距 | `14px 16px` |
| 字号 | `16px` |
| 聚焦 | `border-color: #2E86AB` |
| 聚焦阴影 | `box-shadow: 0 0 0 3px rgba(46, 134, 171, 0.1)` |
| 过渡 | `all 0.3s ease` |
| 标签 | `color: #333`, `font-weight: 600`, `font-size: 15px` |

### 4.3 按钮（Button）

| 属性 | 值 |
|:---|:---|
| 背景 | `linear-gradient(135deg, #2E86AB 0%, #4CAF50 100%)`（主要按钮） |
| 文字颜色 | `white` |
| 圆角 | `border-radius: 12px` |
| 内边距 | `14px 40px` |
| 字号 | `18px` |
| 字重 | `bold` |
| 全宽 | `width: 100%`（登录页）/ 自适应（内部页面） |
| 阴影 | `box-shadow: 0 4px 15px rgba(46, 134, 171, 0.3)` |
| 过渡 | `all 0.3s ease` |

**Hover 状态**:
```css
transform: translateY(-2px);
box-shadow: 0 6px 20px rgba(46, 134, 171, 0.4);
```

**次要按钮（教师端）**:

| 属性 | 值 |
|:---|:---|
| 背景 | `transparent` |
| 边框 | `2px solid #2E86AB` |
| 文字颜色 | `#2E86AB` |
| 圆角 | `border-radius: 12px` |
| 内边距 | `10px 24px` |
| 字号 | `16px` |

**危险按钮（弃用/重置）**:

| 属性 | 值 |
|:---|:---|
| 背景 | `linear-gradient(135deg, #ff4d4f, #ff7875)` |
| 文字颜色 | `white` |
| 圆角 | `border-radius: 12px` |

### 4.4 毛玻璃容器（Glassmorphism）

| 属性 | 值 |
|:---|:---|
| 背景 | `rgba(255,255,255,0.15)` |
| 圆角 | `border-radius: 15px` |
| 内边距 | `padding: 25px` |
| 模糊 | `backdrop-filter: blur(10px)` |
| 适用场景 | 测试账号区域、导航栏、通知条 |

### 4.5 品牌标签（Badge）

```css
position: fixed;
bottom: 20px;
right: 20px;
background: rgba(255,255,255,0.2);
padding: 8px 15px;
border-radius: 20px;
color: white;
font-size: 12px;
backdrop-filter: blur(5px);
```

### 4.6 对话气泡（v2.0 新增）

**用户消息气泡**:

| 属性 | 值 |
|:---|:---|
| 背景 | `linear-gradient(135deg, #2E86AB 0%, #4CAF50 100%)` |
| 文字颜色 | `white` |
| 圆角 | `border-radius: 18px 18px 4px 18px` |
| 最大宽度 | `max-width: 70%` |
| 对齐 | 右对齐（self-end） |

**AI 回复气泡**:

| 属性 | 值 |
|:---|:---|
| 背景 | `white` |
| 边框 | `2px solid #E8E8E8` |
| 文字颜色 | `#333` |
| 圆角 | `border-radius: 18px 18px 18px 4px` |
| 最大宽度 | `max-width: 80%` |
| 对齐 | 左对齐（self-start） |
| 附加 | 顶部显示 Agent 标签 |

**系统消息气泡**:

| 属性 | 值 |
|:---|:---|
| 背景 | `rgba(102, 126, 234, 0.1)` |
| 边框 | `1px dashed #667eea` |
| 圆角 | `border-radius: 12px` |
| 文字颜色 | `#667eea` |
| 文字大小 | `14px` |
| 对齐 | 居中 |

### 4.7 Agent 标签（v2.0 新增）

```css
/* Guide 标签 */
.agent-badge--guide {
  background: linear-gradient(135deg, #667eea, #764ba2);
  color: white;
  border-radius: 12px;
  padding: 4px 12px;
  font-size: 12px;
  font-weight: 600;
  display: inline-flex;
  align-items: center;
  gap: 4px;
}

/* Tutor 标签 */
.agent-badge--tutor {
  background: linear-gradient(135deg, #2E86AB, #4CAF50);
  color: white;
  border-radius: 12px;
  padding: 4px 12px;
  font-size: 12px;
  font-weight: 600;
  display: inline-flex;
  align-items: center;
  gap: 4px;
}
```

### 4.8 求助按钮（HelpButton）— 仅 TCA 组（v2.0 新增）

| 属性 | 值 |
|:---|:---|
| 背景 | `rgba(255,255,255,0.9)` |
| 边框 | `2px solid #2E86AB` |
| 圆角 | `border-radius: 12px` |
| 文字颜色 | `#2E86AB` |
| 字重 | `600` |
| 模糊 | `backdrop-filter: blur(10px)` |
| 图标 | `🆘` 或自定义 SVG |

### 4.9 分隔线

| 属性 | 值 |
|:---|:---|
| 对齐 | `text-align: center` |
| 文字颜色 | `white` |
| 外边距 | `margin: 30px 0 20px 0` |
| 字号 | `16px` |
| 字重 | `500` |

### 4.10 成功/错误消息

| 属性 | 值 |
|:---|:---|
| 圆角 | `border-radius: 10px` |
| 内边距 | `padding: 15px` |
| 居中 | `margin: 15px auto` |
| 最大宽度 | `max-width: 420px` |

---

## 五、四组实验条件 UI 差异化设计

### 5.1 界面组件差异矩阵

| UI 组件 | SA 组 | EXP 组 | AI-AUTO 组 | TCA 组 |
|:---|:---:|:---:|:---:|:---:|
| **学生端对话面板** | ✅ 显示 | ✅ 显示 | ✅ 显示 | ✅ 显示 |
| **Agent 标签显示** | ✅ 显示（固定 Guide） | ✅ 显示（固定 Guide） | ✅ 显示（动态变化） | ✅ 显示（动态变化） |
| **切换原因提示栏** | ❌ 不显示 | ✅ 显示系统模板解释 | ✅ 显示系统模板解释 | ✅ 显示（教师解释或系统） |
| **"请求帮助"按钮** | ❌ 不显示 | ❌ 不显示 | ❌ 不显示 | ✅ **仅此组显示** |
| **教师端监控面板** | ❌ 无 | ❌ 无 | ❌ 无 | ✅ **仅此组显示** |
| **Agent 回切通知** | ❌ 无 | ❌ 无 | ✅ AI 自动回切 | ✅ 教师手动回切 |
| **WebSocket 实时通知** | ❌ 无 | ❌ 无 | ❌ 无 | ✅ 教师端实时推送 |

### 5.2 学生端差异视觉表现

**Agent 标签状态机**:

| 学生所属组 | Agent 初始 | 可切换至 | 切换方式 | 标签显示 |
|:---|:---:|:---:|:---:|:---|
| SA | Guide (固定) | — | 无 | 🟣 Guide（始终紫色） |
| EXP | Guide (固定) | — | 无 | 🟣 Guide（始终紫色） |
| AI-AUTO | Guide | Tutor（自动） | error_streak ≥ 阈值 | 🟣 Guide ↔ 🟢 Tutor |
| TCA | Guide | Tutor（教师决策） | 教师 Override | 🟣 Guide ↔ 🟢 Tutor |

**切换原因提示栏**:

```html
<!-- SA / EXP / AI-AUTO 组不显示 -- 由技术规格说明书的组别判断控制 -->
<!-- TCA 组显示 -->
<div class="switch-reason-bar glass" v-if="condition === 'TCA' && teacherExplanation">
  <span class="switch-reason-icon">💡</span>
  <span class="switch-reason-text">{{ teacherExplanation }}</span>
</div>
```

**帮助按钮条件渲染**:

```html
<!-- 仅 TCA 组显示 -->
<HelpButton v-if="condition === 'TCA'" @click="requestHelp" />
```

---

## 六、登录页面布局

### 6.1 布局结构

```
┌──────────────────────────────────────────────────────┐
│  ████████████████████████████████████████████████████  │  ← 背景渐变 #667eea → #764ba2
│                                                        │
│                    🎓 TCA-System                        │
│   "基于人机协同的初中数学智能学习系统"                   │  ← 白色文字 + 阴影
│                                                        │
│              ┌──────────────────────┐                   │
│              │    📝 用户登录        │                   │  ← 白色卡片, 圆角20px
│              │                      │                   │
│              │   🛜 账号            │                   │
│              │   [━━━━━━━━━━━━━━━] │                   │  ← 圆角12px, 边框#E8E8E8
│              │                      │                   │
│              │   🔒 密码            │                   │
│              │   [━━━━━━━━━━━━━━━] │                   │
│              │                      │                   │
│              │   ┌────────────────┐ │                   │  ← 渐变按钮 #2E86AB→#4CAF50
│              │   │   🔑 登录      │ │                   │
│              │   └────────────────┘ │                   │
│              └──────────────────────┘                   │
│                                                        │
│   💡 首次登录请使用下方测试账号                         │  ← 白色半透明文字
│                                                        │
│   ━━━━━  📋 测试账号信息  ━━━━━                         │  ← 分隔线
│                                                        │
│   ┌──────────────┐   ┌──────────────┐                   │
│   │ 👨‍🎓 学生账号 │   │ 👨‍🏫 教师/管理员│                   │  ← 毛玻璃卡片
│   │  student_sa  │   │ teacher001   │                   │  backdrop-filter: blur(10px)
│   │  ..........  │   │ ..........   │                   │
│   └──────────────┘   └──────────────┘                   │
│                                                        │
│                        🎓 TCA-System·华东师范大学·v2.0          │  ← 右下角毛玻璃标签
└──────────────────────────────────────────────────────┘
```

### 6.2 登录后路由分发

```
登录成功
    ↓
根据 user_type 路由：
    ├── student → 读取 student_assignments.condition → StudentView
    │              （内部根据 condition 控制 UI 差异）
    ├── teacher → TeacherView（仅 TCA 组教师有监控面板）
    └── admin  → AdminView（分组管理、数据导出）
```

---

## 七、学生端页面

### 7.1 布局结构

```
┌──────────────────────────────────────────────────────┐
│  [🎓 TCA-System]  [👤 学生]  [组别标签]  [🚪退出]          │  ← 毛玻璃导航栏
├──────────────────────────────────────────────────────┤
│                                                       │
│   ┌────────────────────────────────────────────┐      │
│   │               对话消息面板                    │      │
│   │   ┌──────────────────────────────────┐      │      │
│   │   │ 🟣 Guide                          │      │      │  ← Agent 标签
│   │   │ "我们先想想这道题考察什么知识点？"    │      │      │  ← AI 气泡 (白色)
│   │   │                           12:30   │      │      │  ← 时间戳
│   │   └──────────────────────────────────┘      │      │
│   │                                             │      │
│   │   ┌──────────────────────────────────┐      │      │
│   │   │    "考察的是三角形内角和定理"      │      │      │  ← 用户气泡 (渐变)
│   │   │                           12:31   │      │      │
│   │   └──────────────────────────────────┘      │      │
│   │                                             │      │
│   │   ┌──────────────────────────────────┐      │      │
│   │   │ 💡 解释提示栏（仅 EXP/TCA 组显示）│      │      │  ← 毛玻璃提示栏
│   │   │ "这道题的关键是看已知条件..."      │      │      │
│   │   └──────────────────────────────────┘      │      │
│   └────────────────────────────────────────────┘      │
│                                                       │
│   ┌────────────────────────────────┐                  │
│   │  [📝 输入你的解答...      ]    │                  │  ← 输入框 圆角12px
│   │  [      📤 发送          ]    │                  │  ← 渐变按钮
│   └────────────────────────────────┘                  │
│                                                       │
│   [🆘 请求教师帮助]  ← 仅 TCA 组显示                    │  ← 毛玻璃按钮
│                                                       │
├──────────────────────────────────────────────────────┤
│  🎓 TCA-System · 华东师范大学 · v2.0                         │
└──────────────────────────────────────────────────────┘
```

### 7.2 Agent 切换通知（AI-AUTO / TCA 组可见）

当 Agent 发生切换时，系统消息显示在对话流中：

```
   ┌─ 系统消息 ───────────────────────────────────┐
   │  🤖 AI检测到学习断点，已切换为 Tutor 模式      │  ← AI-AUTO 组：系统自动
   │  或                                           │
   │  👨‍🏫 老师已为你切换了辅导模式，现在由Tutor来     │  ← TCA 组：教师操作
   │     帮助你                                    │
   └──────────────────────────────────────────────┘
```

### 7.3 教师解释显示（TCA 组）

```
   ┌─ 教师解释 ────────────────────────────────────┐
   │  💡 老师的话：                                  │
   │  "看看题目中的第一个条件，你忽略了什么？"        │
   │  —— teacher_001                               │  ← 教师名称
   └──────────────────────────────────────────────┘
```

---

## 八、教师端页面

> ⚠️ **教师端仅 TCA 组需要**，SA/EXP/AI-AUTO 组不需要教师端。设计依据：技术规格说明书 3.3 节。

### 8.1 布局结构

```
┌──────────────────────────────────────────────────────┐
│  [🎓 TCA-System]  [👨‍🏫 教师]  [📊 监控面板]  [🚪退出]      │  ← 毛玻璃导航栏
├──────────────────────────────────────────────────────┤
│                                                       │
│   ┌──────────────────────────┐  ┌──────────────────┐ │
│   │  📡 学生实时状态列表       │  │  📋 学生详情面板  │ │
│   │                          │  │                  │ │
│   │  ┌────────────────────┐  │  │  🟣 Guide         │ │  ← 当前 Agent
│   │  │ 🎒 student_sa      │  │  │                  │ │
│   │  │   🟣 Guide · 2m前   │  │  │  对话历史(最近5条)│ │
│   │  │   消息: 上次发言内容 │  │  │  ┌────────────┐ │ │
│   │  └────────────────────┘  │  │  │ 学生: ...   │ │ │
│   │                          │  │  │ AI: ...     │ │ │
│   │  ┌────────────────────┐  │  │  └────────────┘ │ │
│   │  │ 🎒 student_tca   🔴│  │  │                  │ │
│   │  │   🟣 Guide · 停滞30s│  │  │  ┌────────────┐ │ │
│   │  │   消息: ...         │  │  │  │🔄切换 Tutor │ │ │  ← Override 按钮
│   │  └────────────────────┘  │  │  │📝发送解释    │ │ │
│   │                          │  │  └────────────┘ │ │
│   │  学生: 12  TCA组: 4     │  │                  │ │
│   │  待处理: 1               │  │  ┌────────────┐ │ │
│   │                          │  │  │待处理请求   │ │ │
│   └──────────────────────────┘  │  │student_tca │ │ │  ← 帮助请求队列
│                                  │  │ 请求: 2m前 │ │ │
│                                  │  └────────────┘ │ │
│                                  │                  │ │
│                                  └──────────────────┘ │
├──────────────────────────────────────────────────────┤
│  🎓 TCA-System · 华东师范大学 · v2.0                         │
└──────────────────────────────────────────────────────┘
```

### 8.2 教师端组件详解

**学生卡片（StudentCard）**:

| 属性 | 值 |
|:---|:---|
| 背景 | `white` |
| 圆角 | `border-radius: 15px` |
| 内边距 | `15px` |
| 阴影 | `box-shadow: 0 4px 15px rgba(0,0,0,0.1)` |
| 左边框 | `4px solid #667eea`（Guide）/ `4px solid #4CAF50`（Tutor） |
| 光标 | `pointer`（点击展开详情） |

**待处理徽章**:

```scss
.pending-badge {
  background: linear-gradient(135deg, #FF9800, #F44336);
  color: white;
  border-radius: 50%;
  min-width: 24px;
  height: 24px;
  padding: 0 6px;
  font-size: 12px;
  font-weight: bold;
  display: flex;
  align-items: center;
  justify-content: center;
  animation: pulse 2s infinite;
}
```

**OverridePanel 组件（v2.0 新增）**:

| 区域 | 内容 | 交互 |
|:---|:---|:---|
| **Agent 切换区** | 当前:🟣 Guide → [切换至Tutor] 按钮 | 点击触发 WebSocket `teacher_override` |
| **解释区** | 文本输入框 + 预设模板选择 | 实时输入或选择模板 → 发送 |
| **操作组合** | 仅切换 / 仅解释 / 切换+解释 / 不干预 | 单选按钮组 |
| **操作日志** | 最近5次 Override 记录 | 只读列表 |

**WebSocket 实时通知条**:

```scss
.notification-bar {
  background: rgba(255, 255, 255, 0.15);
  backdrop-filter: blur(10px);
  border-radius: 12px;
  padding: 12px 20px;
  animation: slideIn 0.3s ease;
}
```

### 8.3 教师端交互流程（UI 层面）

```
触发检测到达教师端
    ↓
教师端右上角显示 Toast 通知："student_tca 触发检测"
    ↓
教师点击学生卡片或通知
    ↓
右侧详情面板展开
    ↓
教师阅读对话历史
    ↓
选择操作：
  ├── 仅切换 Agent → "切换至Tutor" 按钮 → WebSocket 发送
  ├── 仅发送解释 → 输入解释 → "发送" 按钮 → WebSocket 发送
  ├── 切换+解释 → 组合操作 → 两事件先后发送
  └── 不干预 → 关闭面板 → 系统提示"经老师确认，当前的帮助方式适用"
```

---

## 九、Agent 视觉系统

### 9.1 Agent 角色视觉标识

| Agent | 渐变色 | 图标建议 | 角色说明 | 显示位置 |
|:---|:---|:---:|:---|:---|
| **Guide** | `#667eea` → `#764ba2` (紫) | 🧭 / 🧩 | 苏格拉底式提问引导，低支架 | 对话气泡顶部标签 |
| **Tutor** | `#2E86AB` → `#4CAF50` (青绿) | 📚 / 🎯 | 分步讲解，高支架 | 对话气泡顶部标签 |
| **Evaluator** | `#FF9800` → `#F44336` (橙红) | ✅ / 📊 | 答案评估（后端，不对学生展示） | 仅日志 |

### 9.2 Agent 标签样式

```scss
.agent-badge {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 4px 12px;
  border-radius: 12px;
  font-size: 12px;
  font-weight: 600;
  color: white;
  margin-bottom: 6px;

  &--guide {
    background: linear-gradient(135deg, #667eea, #764ba2);
  }

  &--tutor {
    background: linear-gradient(135deg, #2E86AB, #4CAF50);
  }

  &--evaluator {
    background: linear-gradient(135deg, #FF9800, #F44336);
  }
}
```

### 9.3 Agent 切换动画

当 Agent 类型变化时，标签应有平滑过渡：

```scss
.agent-badge {
  transition: background 0.5s ease, transform 0.3s ease;
}

.agent-badge--switching {
  animation: agentSwitchPulse 0.6s ease;
}

@keyframes agentSwitchPulse {
  0% { transform: scale(1); }
  50% { transform: scale(1.15); }
  100% { transform: scale(1); }
}
```

---

## 十、WebSocket 实时通知系统

### 10.1 教师端通知

| 通知类型 | 触发条件 | 表现 | 颜色 |
|:---|:---|:---|:---|
| **trigger_detected** | 触发规则引擎检测到学习断点 | 右上角 Toast | 🟠 橙色边框 |
| **student_help_request** | TCA 学生点击"请求帮助" | 右上角 Toast + 学生卡片闪烁 | 🔴 红色徽章 |
| **new_message** | TCA 学生发送新消息 | 学生卡片消息预览更新 | 灰色 |
| **agent_switched** | Agent 完成切换 | 详情面板 Agent 标签更新 | 🟢 绿色 |

### 10.2 学生端通知

| 通知类型 | 触发条件 | 表现 |
|:---|:---|:---|
| **agent_switched** | Agent 切换完成 | 系统消息插入对话流 |
| **teacher_explanation** | 教师发送解释 | 教师解释卡片插入对话流 |
| **back_to_guide** | 教师/系统回切至 Guide | 系统消息提示回切 |

### 10.3 通知动画样式

```scss
// 滑入动画（教师端右上方通知）
@keyframes slideInFromRight {
  from {
    transform: translateX(100%);
    opacity: 0;
  }
  to {
    transform: translateX(0);
    opacity: 1;
  }
}

// 脉冲动画（待处理徽章）
@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

// 通知条样式
.websocket-toast {
  position: fixed;
  top: 20px;
  right: 20px;
  z-index: 1000;
  min-width: 300px;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(10px);
  border-radius: 12px;
  padding: 16px 20px;
  box-shadow: 0 8px 30px rgba(0, 0, 0, 0.15);
  animation: slideInFromRight 0.3s ease;
  border-left: 4px solid #FF9800;
  cursor: pointer;

  &--help {
    border-left-color: #ff4d4f;
  }

  &--switch {
    border-left-color: #4CAF50;
  }
}
```

---

## 十一、动画与过渡

### 11.1 通用过渡

```scss
// 所有交互元素的默认过渡
$transition-fast: all 0.2s ease;    // hover 反馈
$transition-default: all 0.3s ease;  // 通用
$transition-slow: all 0.5s ease;     // 页面切换

// 按钮 hover
.btn {
  transition: $transition-default;
  &:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(46, 134, 171, 0.4);
  }
}

// 输入框 focus
.input {
  transition: $transition-default;
  &:focus {
    border-color: #2E86AB;
    box-shadow: 0 0 0 3px rgba(46, 134, 171, 0.1);
  }
}
```

### 11.2 页面切换

```vue
<!-- Vue Router 过渡 -->
<router-view v-slot="{ Component }">
  <transition name="fade-slide">
    <component :is="Component" />
  </transition>
</router-view>
```

```scss
.fade-slide-enter-active,
.fade-slide-leave-active {
  transition: all 0.3s ease;
}
.fade-slide-enter-from {
  opacity: 0;
  transform: translateY(20px);
}
.fade-slide-leave-to {
  opacity: 0;
  transform: translateY(-20px);
}
```

### 11.3 对话消息动画（v2.0 新增）

```scss
// 新消息淡入
@keyframes messageIn {
  from {
    opacity: 0;
    transform: translateY(10px) scale(0.98);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

.message-bubble {
  animation: messageIn 0.3s ease forwards;
}

// 消息列表滚动到最新
.message-list {
  scroll-behavior: smooth;
}
```

### 11.4 Agent 标签切换动画

```scss
.agent-badge {
  transition: background 0.5s ease, transform 0.3s ease;
}

// 切换时的闪烁指示
@keyframes switchFlash {
  0% { box-shadow: 0 0 0 0 rgba(102, 126, 234, 0.4); }
  50% { box-shadow: 0 0 0 8px rgba(102, 126, 234, 0); }
  100% { box-shadow: 0 0 0 0 rgba(102, 126, 234, 0); }
}

.agent-switch-flash {
  animation: switchFlash 0.8s ease;
}
```

### 11.5 教师端面板展开动画

```scss
.teacher-detail-panel {
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  
  &--enter {
    transform: translateX(100%);
    opacity: 0;
  }
  
  &--active {
    transform: translateX(0);
    opacity: 1;
  }
}
```

---

## 十二、Element Plus 主题集成

> 基于技术规格说明书 5.2 节：UI 框架选用 Element Plus。

### 12.1 Element Plus 变量覆盖

```scss
// src/styles/element-overrides.scss

// Element Plus 主题色对齐品牌色
$--color-primary: #667eea;
$--color-success: #4CAF50;
$--color-warning: #FF9800;
$--color-danger: #ff4d4f;
$--color-info: #2E86AB;

// 圆角统一
$--border-radius-base: 12px;
$--border-radius-small: 8px;
$--border-radius-medium: 15px;
$--border-radius-large: 20px;

// 字号对齐设计系统
$--font-size-base: 16px;
$--font-size-medium: 15px;
$--font-size-small: 12px;

// 阴影统一
$--box-shadow-base: 0 4px 15px rgba(0, 0, 0, 0.1);
$--box-shadow-light: 0 2px 8px rgba(0, 0, 0, 0.08);
$--box-shadow-dark: 0 15px 50px rgba(0, 0, 0, 0.25);

// 按钮渐变覆盖 (通过自定义类实现，Element Plus 原生不支持渐变按钮)
// 使用自定义组件 GradientButton.vue 替代
```

### 12.2 Element Plus 使用原则

| 场景 | 使用 Element Plus | 使用自定义组件 |
|:---|:---:|:---:|
| 表单输入框 | ✅ `ElInput` + 样式覆盖 | — |
| 普通按钮 | ✅ `ElButton` | — |
| 渐变按钮 | — | ✅ `GradientButton.vue` |
| 下拉选择 | ✅ `ElSelect` | — |
| 表格 | ✅ `ElTable` | — |
| 对话框 | ✅ `ElDialog` | — |
| 通知 | — | ✅ 自定义 WebSocket Toast |
| 对话气泡 | — | ✅ `MessageBubble.vue` |
| Agent 标签 | — | ✅ `AgentBadge.vue` |
| 毛玻璃容器 | — | ✅ `GlassContainer.vue` |

---

## 十三、全局样式与设计 Token

### 13.1 SCSS 变量（Design Token）

```scss
// src/styles/_variables.scss

// ===== 品牌色 =====
$brand-primary-1: #667eea;
$brand-primary-2: #764ba2;
$brand-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);

// ===== Agent 色 =====
$agent-guide-gradient: linear-gradient(135deg, #667eea, #764ba2);
$agent-tutor-gradient: linear-gradient(135deg, #2E86AB, #4CAF50);
$agent-evaluator-gradient: linear-gradient(135deg, #FF9800, #F44336);

// ===== 辅助色 =====
$accent-blue: #2E86AB;
$accent-green: #4CAF50;
$accent-red: #ff4d4f;
$accent-orange: #FF9800;

// ===== 渐变 =====
$btn-gradient: linear-gradient(135deg, $accent-blue 0%, $accent-green 100%);
$user-bubble-gradient: linear-gradient(135deg, $accent-blue 0%, $accent-green 100%);

// ===== 阴影 =====
$shadow-card: 0 15px 50px rgba(0, 0, 0, 0.25);
$shadow-btn: 0 4px 15px rgba(46, 134, 171, 0.3);
$shadow-btn-hover: 0 6px 20px rgba(46, 134, 171, 0.4);
$shadow-text: 2px 2px 4px rgba(0, 0, 0, 0.2);
$shadow-input-focus: 0 0 0 3px rgba(46, 134, 171, 0.1);
$shadow-teacher-card: 0 4px 15px rgba(0, 0, 0, 0.1);

// ===== 文字色 =====
$text-primary: #333333;
$text-secondary: #666666;
$text-on-dark: rgba(255, 255, 255, 0.9);
$text-hint: rgba(255, 255, 255, 0.85);
$text-muted: rgba(255, 255, 255, 0.6);

// ===== 边框 =====
$border-default: #E8E8E8;
$border-focus: $accent-blue;

// ===== 圆角 =====
$radius-card: 20px;
$radius-input: 12px;
$radius-button: 12px;
$radius-glass: 15px;
$radius-glass-sm: 10px;
$radius-bubble: 18px;

// ===== 间距 =====
$spacing-card: 40px;
$spacing-glass: 25px;
$spacing-input-v: 14px;
$spacing-input-h: 16px;
$spacing-button-v: 14px;
$spacing-button-h: 40px;
$spacing-bubble: 12px 18px;
$spacing-cell: 8px 12px;
$spacing-badge: 8px 15px;

// ===== 过渡 =====
$transition-fast: all 0.2s ease;
$transition-default: all 0.3s ease;
$transition-slow: all 0.5s ease;

// ===== 字体 =====
$font-size-hero: 42px;
$font-size-h1: 32px;
$font-size-h2: 24px;
$font-size-h3: 20px;
$font-size-sub: 18px;
$font-size-body: 16px;
$font-size-label: 15px;
$font-size-caption: 12px;

// ===== 组别标识色 =====
$color-group-sa: #9E9E9E;
$color-group-exp: #667eea;
$color-group-ai-auto: #00BCD4;
$color-group-tca: #764ba2;
```

### 13.2 全局样式

```scss
// src/styles/global.scss
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html, body, #app {
  width: 100%;
  height: 100%;
  min-height: 100vh;
}

body {
  font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, 
               "Helvetica Neue", Arial, "Noto Sans SC", sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

// 品牌背景渐变 — 登录页 / 学生端
.brand-bg {
  background: $brand-gradient;
  min-height: 100vh;
}

// 白色卡片
.brand-card {
  background: white;
  border-radius: $radius-card;
  padding: $spacing-card;
  box-shadow: $shadow-card;
  max-width: 420px;
  margin: 0 auto;
}

// 渐变按钮
.btn-gradient {
  background: $btn-gradient;
  color: white;
  border-radius: $radius-button;
  border: none;
  padding: $spacing-button-v $spacing-button-h;
  font-size: $font-size-sub;
  font-weight: bold;
  cursor: pointer;
  box-shadow: $shadow-btn;
  transition: $transition-default;
  width: 100%;
  
  &:hover {
    transform: translateY(-2px);
    box-shadow: $shadow-btn-hover;
  }
  
  &:active {
    transform: translateY(0);
  }
}

// 毛玻璃容器
.glass {
  background: rgba(255, 255, 255, 0.15);
  border-radius: $radius-glass;
  padding: $spacing-glass;
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
}

// 消息列表区域
.chat-area {
  background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
  padding: 20px;
  flex: 1;
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  gap: 12px;
}
```

---

## 十四、组件实现清单

> 基于技术规格说明书 5.6 节前端目录结构，列出各组件的设计参考。

### 14.1 通用组件（src/components/common/）

| 组件名 | 设计参考 | 对应规范 |
|:---|:---|:---:|
| `MessageBubble.vue` | 对话气泡（用户/AI/系统三种样式） | 4.6 |
| `AgentBadge.vue` | Guide/Tutor 渐变标签 + 切换动画 | 9.2 |
| `GradientButton.vue` | 渐变按钮 + hover 上移效果 | 4.3 |
| `GlassContainer.vue` | 毛玻璃容器 | 4.4 |
| `BrandBadge.vue` | 右下角品牌标识 | 4.5 |
| `LoadingSpinner.vue` | 加载旋转器（Element Plus 风格） | — |
| `WebSocketToast.vue` | 实时通知 Toast | 10.3 |
| `GroupBadge.vue` | SA/EXP/AI-AUTO/TCA 组别标识 | 2.4 |
| `ExplanationBanner.vue` | 切换原因提示栏 | 7.2 |

### 14.2 学生端组件（src/components/student/）

| 组件名 | 设计参考 | 对应规范 |
|:---|:---|:---:|
| `ChatPanel.vue` | 对话消息面板，含消息列表+输入区 | 7.1 |
| `AnswerInput.vue` | 答案输入框+发送按钮 | — |
| `HelpButton.vue` | 求助按钮（仅 TCA 组显示） | 4.8 |
| `AgentSwitchNotice.vue` | Agent 切换系统消息卡片 | 7.2 |
| `TeacherExplanation.vue` | 教师解释展示卡片 | 7.3 |

### 14.3 教师端组件（src/components/teacher/）

| 组件名 | 设计参考 | 对应规范 |
|:---|:---|:---:|
| `StudentList.vue` | 学生状态列表，含实时状态更新 | 8.2 |
| `StudentCard.vue` | 学生卡片（含 Agent 状态、消息预览、待处理徽章） | 8.2 |
| `SessionDetail.vue` | 会话详情面板（对话历史+切换日志） | 8.1 |
| `AgentSwitch.vue` | Agent 切换控件 | 8.2 |
| `OverridePanel.vue` | Override 操作面板（切换+解释组合） | 8.2 |
| `PendingBadge.vue` | 待处理请求徽章（脉冲动画） | 8.2 |

### 14.4 视图（src/views/）

| 视图                | 路由         | 包含组件                                                              | 设计参考 |
| :---------------- | :--------- | :---------------------------------------------------------------- | :--: |
| `LoginView.vue`   | `/login`   | BrandCard, GradientButton, BrandInput, GlassContainer, BrandBadge |  六   |
| `StudentView.vue` | `/student` | ChatPanel, AnswerInput, HelpButton, AgentBadge, ExplanationBanner |  七   |
| `TeacherView.vue` | `/teacher` | StudentList, SessionDetail, OverridePanel, WebSocketToast         |  八   |
| `AdminView.vue`   | `/admin`   | 分组管理+数据导出（标准 Element Plus 风格）                                     |  —   |

### 14.5 实现示例：`MessageBubble.vue`

```vue
<template>
  <div
    class="message-bubble"
    :class="[
      `message-bubble--${role}`,
      role === 'assistant' ? `message-bubble--agent-${agent}` : ''
    ]"
  >
    <AgentBadge v-if="role === 'assistant'" :agent="agent" class="mb-1" />
    <div class="message-bubble__content">{{ content }}</div>
    <span class="message-bubble__time">{{ formattedTime }}</span>
  </div>
</template>

<script setup lang="ts">
defineProps<{
  role: 'user' | 'assistant' | 'system';
  content: string;
  agent?: 'Guide' | 'Tutor';
  time: string;
}>();
</script>

<style scoped lang="scss">
.message-bubble {
  max-width: 70%;
  padding: 12px 18px;
  animation: messageIn 0.3s ease forwards;

  &--user {
    align-self: flex-end;
    background: linear-gradient(135deg, #2E86AB 0%, #4CAF50 100%);
    color: white;
    border-radius: 18px 18px 4px 18px;
  }

  &--assistant {
    align-self: flex-start;
    background: white;
    border: 2px solid #E8E8E8;
    border-radius: 18px 18px 18px 4px;
    color: #333;
    max-width: 80%;
  }

  &--system {
    align-self: center;
    background: rgba(102, 126, 234, 0.1);
    border: 1px dashed #667eea;
    border-radius: 12px;
    color: #667eea;
    font-size: 14px;
    max-width: 90%;
    text-align: center;
  }

  &__time {
    font-size: 11px;
    opacity: 0.6;
    display: block;
    margin-top: 4px;
    text-align: right;
  }
}
</style>
```

### 14.6 实现示例：`AgentBadge.vue`

```vue
<template>
  <span class="agent-badge" :class="`agent-badge--${agent.toLowerCase()}`">
    <span class="agent-badge__icon">{{ agentIcon }}</span>
    <span class="agent-badge__label">{{ agent }}</span>
  </span>
</template>

<script setup lang="ts">
import { computed } from 'vue';

const props = defineProps<{
  agent: 'Guide' | 'Tutor' | 'Evaluator';
}>();

const agentIcon = computed(() => ({
  Guide: '🧭',
  Tutor: '📚',
  Evaluator: '✅',
}[props.agent]));
</script>

<style scoped lang="scss">
.agent-badge {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 4px 12px;
  border-radius: 12px;
  font-size: 12px;
  font-weight: 600;
  color: white;
  transition: background 0.5s ease, transform 0.3s ease;

  &--guide {
    background: linear-gradient(135deg, #667eea, #764ba2);
  }

  &--tutor {
    background: linear-gradient(135deg, #2E86AB, #4CAF50);
  }

  &--evaluator {
    background: linear-gradient(135deg, #FF9800, #F44336);
  }
}
</style>
```

---

## 十五、设计规范一致性检查清单

| 检查项 | 说明 | ✅ |
|:---|:---|:-:|
| 页面背景 | 所有学生页/登录页使用 `linear-gradient(135deg, #667eea, #764ba2)` | ⬜ |
| 教师端背景 | 使用浅灰色渐变 `#f0f2f5 → #e8ecf1` | ⬜ |
| 卡片样式 | `border-radius: 20px`, `white` 背景, `box-shadow` | ⬜ |
| 输入框 | `border-radius: 12px`, 聚焦边框 `#2E86AB` | ⬜ |
| 主要按钮 | `linear-gradient(135deg, #2E86AB, #4CAF50)`, hover 上移 | ⬜ |
| 次要按钮 | 透明背景 + `#2E86AB` 边框 | ⬜ |
| 毛玻璃 | `backdrop-filter: blur(10px)`, `rgba(255,255,255,0.15)` | ⬜ |
| 品牌标签 | 右下角 `position: fixed`, 半透明毛玻璃 | ⬜ |
| 字体 | 白色（暗底）/ `#333`（亮底） | ⬜ |
| 对话气泡 | 用户：渐变，AI：白色边框，系统：虚线边框 | ⬜ |
| AgentBadge | Guide: 紫色渐变, Tutor: 青绿渐变, Evaluator: 橙红渐变 | ⬜ |
| Agent 切换动画 | 标签有 pulse/scale 过渡动画 | ⬜ |
| 帮助按钮 | `backdrop-filter` 毛玻璃 + `#2E86AB` 边框（仅 TCA 组） | ⬜ |
| 教师学生卡片 | 白色背景 + 左边框颜色对应 Agent 类型 | ⬜ |
| 待处理徽章 | 橙红渐变 + 脉冲动画 | ⬜ |
| WebSocket 通知 | 右侧滑入 + 左边框颜色区分类型 | ⬜ |
| 组别标识色 | SA:灰 / EXP:蓝 / AI-AUTO:青 / TCA:紫 | ⬜ |
| 过渡 | `all 0.3s ease`，消息有 messageIn 动画 | ⬜ |
| Element Plus | 主题色对齐品牌色，圆角统一 | ⬜ |
| 条件渲染 | SA/EXP/AI-AUTO 不显示帮助按钮和教师端 | ⬜ |

---

## 附录A：与旧版 Streamlit 到 Vue3 对照

| Streamlit CSS（v1.0 来源） | Vue3 SCSS（v2.0） | 变更说明 |
|:---|:---|:---|
| `.stApp { background: linear-gradient(...) }` | `.brand-bg` | 保留，全局类 |
| `div[data-testid="stForm"]` | `.brand-card` | 保留，通用组件 |
| `.stTextInput > div > div > input` | `.input-brand` / `ElInput` | 新增 Element Plus 覆盖方案 |
| `.stFormSubmitButton > button` | `.btn-gradient` / `GradientButton.vue` | 保留，通用组件 |
| `.test-accounts` | `.glass` | 保留，通用组件 |
| — | `.message-bubble--user` | **新增** — 对话气泡用户端 |
| — | `.message-bubble--assistant` | **新增** — 对话气泡 AI 端 |
| — | `.message-bubble--system` | **新增** — 系统消息 |
| — | `.agent-badge--guide` | **新增** — Guide 标签 |
| — | `.agent-badge--tutor` | **新增** — Tutor 标签 |
| — | `.agent-badge--evaluator` | **新增** — Evaluator 标签 |
| — | `.websocket-toast` | **新增** — WebSocket 通知条 |
| — | `.teacher-detail-panel` | **新增** — 教师端详情面板 |
| — | `.pending-badge` | **新增** — 待处理徽章 |
| — | `.switch-reason-bar` | **新增** — 切换原因提示栏 |
| — | `.override-panel` | **新增** — Override 操作面板 |
| `.brand-badge` | `.brand-badge` | 保留 |

---

*本文档 v2.0 — 基于 `TCA-System_技术规格说明书.md`（V2.0）重构，覆盖 Vue3 + FastAPI + MySQL 8 新技术栈的四组实验条件设计、三 Agent 视觉系统、WebSocket 实时通知及教师 Override 面板。*

*建议在 Vue3 项目初始化时：*
1. *创建 `src/styles/_variables.scss`（设计 Token）*
2. *创建 `src/styles/global.scss`（全局样式）*
3. *创建 `src/styles/element-overrides.scss`（Element Plus 主题覆盖）*
4. *在 `main.ts` 中全局引入 SCSS 文件*
5. *按 14.1-14.3 的组件清单逐步实现通用组件*